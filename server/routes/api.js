const express = require('express');
const pokeController = require('../controllers/pokeController');
const router = express.Router();

// router.get('/', pokeController.getPoke, (req, res) => res.status(200).json());

// module.exports = router;